# Merge ready

The functional preview built successfully. The database and Edge Function changes are already applied to the connected Supabase project. Merge this branch only after the latest documentation-only preview is no longer queued.
